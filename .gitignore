node_modules/
dist/
*.local

# Sample plans are archival material and stay in the private repo
*.XEL
*.xel

# Font pack is redistributable but bulky; extract-font.js regenerates
# src/render/font-data.js from it. Keep the pack out of the repo.
font/

# Product database is a third party's data; keep it local only
*.BIN
